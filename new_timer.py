import time
import datetime

class my_timer():

    def __init__(self):
        self.a = 0

    # def delete_remind(id_sched):
        


    # def get_list_remind():
    #     return scheduler.queue

    def create_remind(self,date):
        
        

        run_reminder(remind_)
            
    def create_loop_remind(self,time_loop):
        scheduler = sched.scheduler(time.time,
                                time.sleep)
        scheduler.every(time_loop).second.do(send_remind)

        # function to print time and
        # message of the event 
    def send_remind(self,message):
        print('EVENT:', time.time(), message)

            # printing starting time
        print ('START:', time.time())

    def start_reminder(self,id):
        while true:
            get
            time.sleep(5)